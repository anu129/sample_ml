
steps to follow
1. Connect to GIT REPO
2. create virtual evvironment
3. Create readme.md
4. git config user.email are used to set your identity in Git.  ( for team projects , to set identity )
5. git branch -m main ( for renaming master branch to main)
6. git commit -m "comment" ( staging the change)
7. git push ( This is the core command that uploads your local commits to a remote server.
8. git push -u origin main) ( do this one time in a project to setup connect with upstream)
9. git push ( for rest of the commit)